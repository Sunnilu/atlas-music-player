// src/types.ts

export interface Song {
    id: number;
    title: string;
    artist: string;
    duration: string;
    // Add more fields here if needed from the API (e.g., url, albumArt, etc.)
  }
  